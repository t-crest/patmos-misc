24:
p2 is never used after this line, so might as well use it instead of moving its value into p4 and using that.

42: 
like with line 24, p3 is neve used again, so might as well just use p3 instead of p4



